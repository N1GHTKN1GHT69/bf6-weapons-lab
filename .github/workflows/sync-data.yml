name: Sync BF6 source data (legacy manual)
on:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: bf6-data-pipeline
  cancel-in-progress: false
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Source sync is now atomic with cache generation."
          echo "Run the 'BF6 Combat Engine' workflow instead."
